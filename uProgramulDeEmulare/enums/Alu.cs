namespace uProgramulDeEmulare.enums
{
    public enum Alu
    {
        NONE = 0,
        SBUS = 1,
        DBUS = 2,
        ADD = 3,
        SUB,
        AND,
        OR,
        XOR,
        ASL,
        ASR,
        LSR,
        ROL,
        ROR,
        RLC,
        RRC,
    }
}
